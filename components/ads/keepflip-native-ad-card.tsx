import type { StyleProp, ViewStyle } from 'react-native';

type KeepFlipNativeAdCardProps = {
  placement?: string;
  style?: StyleProp<ViewStyle>;
};

export function KeepFlipNativeAdCard(_props: KeepFlipNativeAdCardProps) {
  return null;
}
