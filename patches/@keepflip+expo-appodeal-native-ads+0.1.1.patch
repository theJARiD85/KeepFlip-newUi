diff --git a/node_modules/@keepflip/expo-appodeal-native-ads/plugin/build/withAppodealNativeAds.js b/node_modules/@keepflip/expo-appodeal-native-ads/plugin/build/withAppodealNativeAds.js
index 0000000..0000000 100644
--- a/node_modules/@keepflip/expo-appodeal-native-ads/plugin/build/withAppodealNativeAds.js
+++ b/node_modules/@keepflip/expo-appodeal-native-ads/plugin/build/withAppodealNativeAds.js
@@ -30,6 +30,9 @@ const APPODEAL_REPOSITORIES = [
 
 const ADMOB_ADAPTER =
   'implementation("com.appodeal.ads.sdk.adapters:admob:25.2.0.0")';
+const BIDON_ADAPTER =
+  'implementation("com.appodeal.ads.sdk.adapters:bidon:0.14.0.0")';
+const ADMOB_DEPENDENCIES = [ADMOB_ADAPTER, BIDON_ADAPTER];
 
 function repositoryBlock(repository, indent = "    ") {
   const contentLines = repository.groups
@@ -76,8 +79,11 @@ function addRepositories(source) {
 }
 
 function addAdMobAdapter(source) {
-  if (source.includes(ADMOB_ADAPTER)) {
+  const missingDependencies = ADMOB_DEPENDENCIES.filter(
+    (dependency) => !source.includes(dependency),
+  );
+  if (missingDependencies.length === 0) {
     return source;
   }
 
@@ -91,7 +97,7 @@ function addAdMobAdapter(source) {
   return source.replace(
     dependenciesPattern,
     (match) =>
-      `${match}\n    // Optional Appodeal AdMob adapter.\n    ${ADMOB_ADAPTER}`,
+      `${match}\n    // Optional Appodeal AdMob adapter and its Bidon mediation bridge.\n    ${missingDependencies.join("\n    ")}`,
   );
 }
